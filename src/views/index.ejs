<%- include('includes/homeInclude'); %>
<body>
    <div class="container">
    <h2>Formulário do Ti:</h2>
    <form action="/" method="post">
        <label>
            <% if(typeof titulo !== 'undefined'){ %>
               <%= titulo%>
            <% } else {%> 
                Qual é o seu nome?
            <% } %>
        </label><br>
        <input type="text" name ="pessoa"><br>
        <button>Enivar! ç.ç </button>
    </form>
    </div>
    <script src="./assets/js/bundle.js"></script>
</body>
</html>